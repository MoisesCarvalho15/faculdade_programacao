// criando um array vazio
var alunos = []

// adicionando elementos no array
var alunos = new Array('Alex', 'Ana', 'João')

// acessando um array através de seu índice
console.log(alunos[0])

// Imprimindo todos os elementos do array
for (aluno in alunos){
    console.log(alunos[aluno])
}
