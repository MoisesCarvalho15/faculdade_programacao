var nomes = [];

// adicionando elementos através do push
nomes.push('Moisés', 'Carlos', 'Patrícia');

console.log(nomes);

// Verificando a quantidade de elementos de um array
console.log('Quantidade de nomes na variável: ' + nomes.length);
